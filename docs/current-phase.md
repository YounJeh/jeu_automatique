Current phase: PHASE 8
Goal: Rendre la qualité des jeux (générés ou presets) mesurable plutôt que déduite de la seule validation statique : ajouter un simulateur headless à métriques agrégées (src/game/simulation/), une suite de scénarios déterministes ciblés, un dataset d'évaluation versionné (tests/evals/game-generation-cases.json) avec runner CI (génération mockée), des assertions de simulation sur les cas jouables, et une trace structurée minimale par génération dans le workflow Mastra (generationId, mécaniques sélectionnées, réparation/repli, résultat de chargement runtime) — sans base de données ni système d'observabilité externe.
Exit criteria: SimulationMetrics + runHeadlessSimulation déterministes (même seed -> mêmes métriques) sur les définitions GenericRuntime-capables, restreints par isGenericRuntimeCapable (shoot exclu, cf. docs/mechanics/shoot.md) ; suite de scénarios déterministes couvrant collision joueur/collectible -> score, health=1 + collision ennemi -> lost, timer-expired + objectif survive -> won, et projectile/ennemi -> ennemi supprimé (ce dernier contre ShooterEngine, seule voie d'exécution de shoot) ; tests/evals/game-generation-cases.json couvrant au moins les 9 catégories CLAUDE.md §15.4, validé par schéma Zod et exécuté par un runner mocké dans pnpm test (régressions détectables en CI, sans appel modèle réel) ; cas dodge/collect du dataset exécutant une simulation réelle et assertant runtimeErrors === 0 ; chaque génération (flag ON et OFF) émet une ligne de log structurée sans prompt ni raisonnement ; pnpm check et pnpm test:e2e passent flag activé et flag désactivé.
