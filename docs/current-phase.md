Current phase: PHASE 6
Goal: Construire un runtime générique capable d'exécuter une GameDefinition (mouvement, spawn, collision, déclenchement et exécution de règles, transitions won/lost, lifecycle), avec un RNG injectable et un état runtime distinct de la définition statique, sans remplacer les moteurs existants (dodge/collect/shooter restent le chemin de jeu réel jusqu'à validation complète).
Exit criteria: runtime générique fonctionnel ; cycle de vie fiable ; RNG injectable ; états séparés (GameDefinition jamais mutée) ; dodge et collect migrés et prouvés équivalents aux moteurs existants, shooter en compatibilité claire documentée (mécanique shoot différée, collision N:M projectile/ennemi hors périmètre) ; anciens tests inchangés ; aucune dépendance au LLM dans le runtime ; pnpm check et pnpm test:e2e passent.
