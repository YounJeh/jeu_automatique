Current phase: PHASE 7
Goal: Faire du runtime générique la voie d'exécution principale : transformer dodge/collect/shooter en presets GameDefinition (src/game/presets/), câbler GenericRuntime + GenericRenderer dans GameController et le workflow Mastra (mechanics → GameDefinition, une réparation structurée, repli déterministe sur preset), le tout derrière un flag GENERIC_RUNTIME_ENABLED par défaut désactivé, sans supprimer les moteurs/pipeline existants tant que la voie générique n'est pas validée en production.
Exit criteria: dodge, collect et shooter représentables par GameDefinition (3 presets validés schéma + sémantique + jouabilité) ; GameController joue dodge/collect via GenericRuntime flag activé, avec repli fiable vers les moteurs legacy sinon (flag désactivé, definition absente, ou mécanique non supportée) ; shooter reste sur ShooterEngine — la mécanique shoot (spawn déclenché par input, collision N:M projectile/ennemi) n'est pas exécutable par GenericRuntime, isGenericRuntimeCapable() l'exclut explicitement, documenté dans docs/mechanics/shoot.md comme incrément séparé ; le critère « plus de moteurs différents » est donc satisfait à 2/3 seulement, assumé et documenté plutôt que forcé ; le workflow Mastra produit des structured outputs GameDefinition avec validation stricte (schéma, sémantique, jouabilité) avant sauvegarde, une réparation maximum puis repli sur le preset le plus proche parmi ceux exécutables par GenericRuntime (jamais shooter) ; l'ancien pipeline classify → config reste intact et inchangé flag désactivé ; pnpm check et pnpm test:e2e passent flag activé et flag désactivé.
